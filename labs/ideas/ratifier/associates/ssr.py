class SalesforceServiceRegistry(object):
    @staticmethod
    def create_service_defintion(repod, service_file):
        print("Creating a service defintion in repository={0} for service={1}".format(repod, service_file))
